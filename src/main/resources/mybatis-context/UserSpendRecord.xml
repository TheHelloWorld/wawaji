<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lzg.wawaji.dao.UserSpendRecordDao">

    <!-- 用户消费记录表 -->
    <resultMap type="com.lzg.wawaji.entity.UserSpendRecord" id="userSpendRecordMap">
        <!-- 主键id -->
        <id property="id" column="id" jdbcType="BIGINT"/>
        <!-- 用户编号 -->
        <result property="userNo" column="user_no" jdbcType="VARCHAR"/>
        <!-- 消费游戏币数 -->
        <result property="coin" column="coin" jdbcType="INTEGER"/>
        <!-- 交易类型 -->
        <result property="tradeType" column="trade_type" jdbcType="INTEGER"/>
        <!-- 交易状态 -->
        <result property="tradeStatus" column="trade_status" jdbcType="INTEGER"/>
        <!-- 交易日期 -->
        <result property="tradeDate" column="trade_date" jdbcType="INTEGER"/>
        <!-- 交易时间 -->
        <result property="tradeTime" column="trade_time" jdbcType="DATE"/>
        <!-- 创建时间 -->
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <!-- 最后修改时间 -->
        <result property="lastUpdateTime" column="last_update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!-- 添加用户消费记录 -->
    <insert id="addUserSpendRecord" parameterType="com.lzg.wawaji.entity.UserSpendRecord">
        insert into user_spend_record
            (
                user_no,
                coin,
                trade_type,
                trade_status,
                trade_date,
                trade_time
            )
        values
            (
                #{userNo, jdbcType=VARCHAR},
                #{amount, jdbcType=DECIMAL},
                #{tradeType, jdbcType=INTEGER},
                #{tradeStatus, jdbcType=INTEGER},
                #{tradeDate, jdbcType=INTEGER},
                #{tradeTime, jdbcType=DATE}
            )
    </insert>

    <!-- 根据用户编号分页获得用户消费记录 -->
    <select id="getUserSpendRecordByUserNo" resultMap="userSpendRecordMap">
        select
            id,
            user_no,
            coin,
            trade_type,
            trade_status,
            trade_date,
            trade_time
        from user_spend_record
        where user_no = #{userNo}
        limit #{startPage}, #{pageSize}
    </select>

</mapper>